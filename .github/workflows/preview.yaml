on: 
  pull_request:
    branches:
      - master

jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@master

    - name: Login
      uses: okteto/login@master
      with:
        token: ${{ secrets.OKTETO_TOKEN }}

    - name: Create namespace
      uses: okteto/create-namespace@master
      with:
        namespace: pr-${{ github.event.number }}-cindylopez

    - name: Deploy Application
      uses: okteto/pipeline@master
      with:
        namespace: pr-${{ github.event.number }}-cindylopez
        timeout: 10m

    - name: comment PR
      uses: okteto/notify-pr@80d2be9b47eaa42fed108753a3bc86098b20e236
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        msg: "Preview environment available at https://movies-pr-${{ github.event.number }}-cindylopez.cloud.okteto.net"
